// Types the Compose compiler cannot prove stable, but which this app only ever uses immutably.
//
// Every mesh list here is a fresh snapshot — built by createVertices, mapIndexed, plus/minus, or
// decoded from JSON — and never mutated in place, so equals() reflects content and the compiler
// can compare by value instead of by reference.
kotlin.collections.List
kotlin.collections.Set
